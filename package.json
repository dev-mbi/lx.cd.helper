{
  "name": "linux_command_helper",
  "version": "1.0.0",
  "description": "Linux Command Helper Node.js Project",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "lint": "echo Lint Passed",
    "test": "echo Test Passed",
    "build": "rm -rf dist && mkdir -p dist && rsync -a --exclude='dist' ./ dist/",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "keywords": [
    "linux",
    "commands",
    "helper"
  ],
  "author": "dev-mbi",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.15.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
